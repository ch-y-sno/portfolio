<div class="grid grid-cols-5 gap-4">
  <% topic_articles.each do |topic_article| %>
    <div>
      <%= link_to image_tag(topic_article.article_image.url, class: "h-auto max-w-full rounded-lg"), article_path(topic_article) %>
    </div>
  <% end %>
</div>
