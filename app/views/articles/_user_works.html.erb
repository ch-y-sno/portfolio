<% if @my_articles.present? %>
  <%= render 'articles/search_form', q: @q, url: home_path %>
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
    <%= render partial: 'articles/article', collection: @my_articles %>
  </div>
  <%= paginate @my_articles %>
<% else %>
  <p class="mb-3">
    <%= t('.no_result') %>
  </p>
<% end %>
